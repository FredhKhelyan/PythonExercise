# Hangman (terminal)

This is a small terminal Hangman game written in Python.

How to run

1. Make sure you have Python 3 installed.
2. From the repository root (where `hangman.py` is located) run:

```bash
python hangman.py
```

Notes
- The game runs in the terminal and uses simple text input.
- No external dependencies required.

Enjoy!
